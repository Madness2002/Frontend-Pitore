import { SearchIteracionPipe } from './search-iteracion.pipe';

describe('SearchIteracionPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchIteracionPipe();
    expect(pipe).toBeTruthy();
  });
});
